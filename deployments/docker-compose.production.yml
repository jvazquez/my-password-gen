version: '3.5'
services:
  my-password-gen:
    image: local-my-password-gen
    container_name: my-password-gen-api
    volumes:
      - j-vazquez.com:/app
networks:
  default:
    external:
      name: j-vazquez.com
volumes:
  j-vazquez.com:
    external: true
